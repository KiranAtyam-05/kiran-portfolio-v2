const fs = require('fs');
const path = require('path');
const zlib = require('zlib');

console.log('Replace public/resume/resume.txt with plain resume text. For DOCX extraction, use Word/Google Docs export to text, then paste here.');
